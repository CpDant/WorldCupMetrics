import React from 'react';
import MainLayout from '../Layout/MainLayout';

const Crud = () => {
    return (
        <MainLayout>
            <h1>Crud operations</h1>
            <p>Here there will be the table with the possibility to create, read, update or delete a collection in a document.</p>
        </MainLayout>
    );
};

export default Crud;