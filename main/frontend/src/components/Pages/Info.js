import React from 'react';
import MainLayout from '../Layout/MainLayout';

const Info = () => {
    return (
        <MainLayout>
            <h1>Info about the project</h1>
            <p>Here we will explain, the scope of the project and the specification of the datasets used for the project.</p>
        </MainLayout>
    );
};

export default Info;